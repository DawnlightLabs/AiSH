name: AI Run Routing Contract

on:
  pull_request:
    branches:
      - main
    paths:
      - apps/provider-shell/**
      - crates/aish-ai/**
      - crates/aish-provider/**
      - .github/workflows/apply-ai-run-routing-fix.yml
  push:
    branches:
      - main
    paths:
      - apps/provider-shell/**
      - crates/aish-ai/**
      - crates/aish-provider/**
      - .github/workflows/apply-ai-run-routing-fix.yml

permissions:
  contents: read

jobs:
  routing-contract:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo fmt --all --check
      - run: cargo test -p aish-ai -p aish-provider -p aish-provider-shell
      - run: cargo check --workspace
